services:
  app:
    image: ivangorban/smail-backend
    env_file:
      - .env
    ports:
      - "8080:8080"
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped
  postgres:
    image: postgres:16.3
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DBNAME}
      PGDATA: /var/lib/postgresql/data/pgdata
    volumes:
      - postgres_data:/var/lib/postgresql/data/pgdata
    healthcheck:
      test:
        ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DBNAME}"]
      interval: 30s
      timeout: 10s
      retries: 5
    restart: unless-stopped
  minio:
    image: minio/minio:RELEASE.2025-09-07T16-13-09Z
    environment:
      MINIO_ROOT_USER: ${MINIO_ACCESS_KEY}
      MINIO_ROOT_PASSWORD: ${MINIO_SECRET_KEY}
    command: server --console-address ":9090" /data
    volumes:
      - minio_data:/data

  postfix:
    build:
      context: .
      dockerfile: microservices/postfix/Dockerfile
    volumes:
      - ./configs/postfix/main.cf:/etc/postfix/main.cf:ro
      - ./configs/postfix/transport:/etc/postfix/transport:ro
      - ./certs/postfix:/etc/ssl/postfix:ro
      - postfix_queue:/var/spool/postfix
    ports:
      - "25:25"
      - "587:587"
    hostname: mail.e-smail.ru
    restart: unless-stopped

volumes:
  postgres_data:
  minio_data:
  postfix_queue:
