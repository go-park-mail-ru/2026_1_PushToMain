global:
  scrape_interval: 15s
  external_labels:
    env: dev

scrape_configs:
  - job_name: "node"
    static_configs:
      - targets: ["node_exporter:9100"]

  - job_name: "app"
    static_configs:
      - targets:
          - "app:8080"
        labels:
          group: "backend"
